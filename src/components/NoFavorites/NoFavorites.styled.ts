import styled from "styled-components";

export const StyledNoFavoritesWrapper = styled.div`
  display: block;
  width: 688px;
  padding: 68px;
  border-radius: 30px;
  margin-left: auto;
  margin-right: auto;
  background: var(--white);
  border: 1.5px dashed var(--yellow);
`;

export const StyledNoFavoritesImg = styled.img`
  display: block;
  width: 200px;
  border-radius: 30px;
  margin-left: auto;
  margin-right: auto;
  margin- bottom: 38px;
`;

export const StyledNoFavoritesText = styled.h3`
  font-size: 40px;
  font-weight: var(--medium);
  line-height: 1.2;
  letter-spacing: -0.8px;
`;
